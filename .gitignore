# Ignore direnv runtime artifacts
.direnv/

# Nix build symlinks
result
result-*

# Editable virtualenvs / caches (helpful defaults)
.venv/
.python-version
.pytest_cache/
node_modules/
.DS_Store

# Ignore nix-direnv / direnv runtime files when they end up inside .devenv
.devenv/profile
.devenv/run
.devenv/state
.devenv/input-paths.txt
.devenv/load-exports
.devenv/nix-eval-cache.db*
.devenv/shell-*.sh
.devenv/nixpkgs-config-*.nix
target/
# Claude Code local settings and runtime files
.claude/

# G1 WS-B (docs/graph-ui-g1-handoff.md): ui/ のビルド成果物。`strata site` が
# 合成する入力であり、ソースではないのでコミットしない(pnpm build で再生成する)。
ui/dist/
